# mas245-vendingmachine

This repository is a basic C++ class-usage example and a teaching example for
the MAS245 course.

## Build and run with CMake

```sh
cmake -S . -B build
cmake --build build
./build/VendingMachine
```
